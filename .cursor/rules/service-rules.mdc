---
description: OPENCLAW_FS 项目服务启停规范
alwaysApply: true
---

# 服务启停规范

## 启停命令

项目提供了统一的启停脚本，必须使用以下命令管理服务：

### 启动服务

```bash
./start.sh
```

启动脚本会依次启动：
1. Gateway (后端) - 端口 18789
2. Control UI (前端) - 端口 5173

### 停止服务

```bash
./stop.sh
```

停止脚本会：
1. 停止 Gateway 进程
2. 停止 Control UI 进程
3. 清理可能残留的浏览器控制进程

## 日志查看

```bash
# 查看 Gateway 日志
tail -f /tmp/openclaw/openclaw-YYYY-MM-DD.log

# 查看 Control UI 日志
tail -f /tmp/openclaw/openclaw-ui-YYYY-MM-DD.log
```

## 注意事项

1. **必须使用脚本**: 所有服务的启停都必须通过 `start.sh` 和 `stop.sh` 进行，禁止直接使用 `pnpm openclaw gateway` 启动
2. **检查进程**: 如需确认服务状态，可使用以下命令：
   ```bash
   ss -ltnp | grep -E "18789|5173"
   ```
3. **端口占用**: 如果端口被占用，启动脚本会自动尝试停止占用进程

## 服务地址

| 服务 | 端口 | 说明 |
|------|------|------|
| Gateway | 18789 | 后端 WebSocket 服务 |
| Control UI | 5173 | 前端开发服务器 |
